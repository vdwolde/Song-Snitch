# Handoff — {{Date}}

<!-- Written on request at the end of a work session, read at the start of the next.
     Not auto-generated, not appended to — each save replaces the file. Owns only the
     state of THIS project's work in flight; durable cross-session facts (how you work,
     corrections, why a constraint exists) belong in the native memory store instead —
     see the root CLAUDE.md § Carrying context between sessions. -->

## State

<!-- What's done, what isn't. Files, branches, PR/MR numbers, decisions. 2-4 lines. -->

## Next

<!-- What to pick up, in priority order. 1-3 items. -->

## Context

<!-- Non-obvious gotchas, blockers, preferences from this session. Omit this section if
     there's nothing to add. -->

<!-- Rules: under 20 lines total. Be specific — paths, branch names, ticket numbers, not
     vague summaries. Forward-looking — the next session doesn't care about the journey,
     only what's left. If there's nothing meaningful to hand off, replace the whole file
     body with a single line: "No active work." -->
